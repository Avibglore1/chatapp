{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm install @rollup/rollup-linux-x64-gnu && npm run build"
        }
      }
    ]
  }